apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
spec:
  clusterIP: None        # this is what makes it Headless!
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432