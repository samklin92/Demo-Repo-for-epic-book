apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-epick-book.fullname" . }}-test-connection"
  labels:
    {{- include "my-epick-book.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-epick-book.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
